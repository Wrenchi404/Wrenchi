{
  "name": "wrenchi",
  "version": "2.0.2",
  "description": "Wrenchi da bot",
  "main": "./dist",
  "author": "Wrench",
  "license": "MIT",
  "repository": "git+https://github.com/WrenchWD/Wrenchi.git",
  "scripts": {
    "dev": "npm run build && node dist --dev",
    "build": "npm run rm:build && tsc",
    "rm:build": "rm -rf dist",
    "start": "npm run build && node dist",
    "prod": "npm run build && node dist --prod"
  },
  "dependencies": {
    "@discordjs/builders": "^0.12.0",
    "@discordjs/rest": "^0.3.0",
    "colorette": "^2.0.16",
    "discord-api-types": "^0.29.0",
    "discord.js": "^13.6.0",
    "dotenv": "^16.0.0",
    "erela.js": "^2.3.3",
    "mongoose": "^6.2.9"
  }
}
