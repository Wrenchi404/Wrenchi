{
  "name": "wrenchi",
  "version": "2.5.0",
  "description": "Wrenchi da bot",
  "main": "dist",
  "author": "Wrench",
  "license": "MIT",
  "repository": "git+https://github.com/WrenchWD/Wrenchi.git",
  "scripts": {
    "dev": "npm run build && node dist/src --dev",
    "build": "npm run rm:build && tsc",
    "rm:build": "rm -rf dist",
    "start": "npm run build && node dist/src",
    "prod": "npm run build && node dist/src --prod",
    "destroy": "npm run build && tsc && node dist/handlers/destroyCommands.js"
  },
  "dependencies": {
    "@999-cs/game-stats": "^1.0.3",
    "@discordjs/builders": "^0.12.0",
    "@discordjs/rest": "^0.3.0",
    "@types/fluent-ffmpeg": "^2.1.20",
    "colorette": "^2.0.16",
    "discord-api-types": "^0.29.0",
    "discord.js": "^13.8.1",
    "dotenv": "^16.0.0",
    "erela.js": "^2.3.3",
    "fluent-ffmpeg": "^2.1.2",
    "mongoose": "^6.2.10",
    "mysql2": "^2.3.3",
    "readline": "^1.3.0",
    "ytdl-core": "^4.11.0"
  }
}
